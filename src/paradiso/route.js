import paradiso from "../paradiso"

class Route {
  constructor(routes) {
    return this.route.store(routes)
  }

  set(routes) {
  	return this.route.store(routes)
  }
}

export default paradiso(Route)
